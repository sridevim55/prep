#include<stdio.h>
int main(){
    int a,temp,sum=0,rem=0;
    scanf("%d",&a);
    temp=a;
    while(a!=0){
        rem=a%10;
        sum=(sum*10)+rem;
        a=a/10;
        
    }
    (temp==sum)?printf("palindrome"):printf("not");
    return 0;
}
